import Registration from "@/components/registration/registration";

export default function RegistrationPage() {
    return (
        <main className="bg-[url('/svg/background-grid.svg')] bg-center overflow-hidden w-full min-h-lvh flex items-center justify-center">
            <Registration />
        </main>
    );
}
